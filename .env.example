# Ecomail API credentials (required for /api/subscribe)
# Get these from your Ecomail account → Settings → API keys
ECOMAIL_API_KEY=
ECOMAIL_LIST_ID=

# CRM webhook (optional — skipped when missing)
# Supabase Edge Function for prospect tracking
CRM_WEBHOOK_URL=
CRM_WEBHOOK_TOKEN=
